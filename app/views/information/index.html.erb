
<ul>
	<% used_currentdate = "" %>
<% @information.each do |information| %>

	<% t = Time.now %>

	<% current_date = t.strftime('%d %b %y') %>

	<% if used_currentdate != current_date %>
		<h1 style='color:#cfcfcf'><%= current_date %></h1>
	<% end %>

	<% used_currentdate = current_date %>

	<li style='list-style:none'><h2><span style='font-size:0.7em;color:#bababa'><%= information.created_at.hour %>:<%= information.created_at.to_s.to_time.strftime('%M') %></span> <%= information.nugget %></h2></li>
 
<% end %>
</ul>
